# URL base de Supabase (Project settings → API → Project URL)
SUPABASE_URL=https://TU-PROYECTO.supabase.co

# anon key (NUNCA la service_role en frontend; aquí basta anon)
SUPABASE_ANON_KEY=eyJhbGciOi...

# Puerto (Railway lo inyecta, pero por si corres local)
PORT=8080

# URL pública de este backend en Railway (ajústala tras el primer deploy)
PUBLIC_BASE_URL=https://tu-servicio.up.railway.app
